# Required for ms_user service
DATABASE_URL="your_database_connection_string"
CLERK_SECRET_KEY="your_clerk_secret_key"
CLERK_WEBHOOK_SECRET="your_clerk_webhook_secret"

# Optional: Max request body size for webhooks in MB. Defaults to 1MB.
WEBHOOK_MAX_BODY_SIZE_MB=1
# Optional: Default storage quota for new users in GB. Defaults to 5GB.
DEFAULT_STORAGE_QUOTA_GB=5